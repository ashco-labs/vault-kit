# vault-kit environment configuration
# Copy to .env.local and fill in values, or use op run with op:// references.

# Claude proxy (preferred, uses Max plan, zero marginal cost)
CLAUDE_PROXY_URL="http://127.0.0.1:8791"
CLAUDE_PROXY_API_KEY="op://Agents/Claude Proxy API Key/credential"

# Direct Anthropic API (fallback if proxy unavailable)
# ANTHROPIC_API_KEY="op://Agents/Anthropic API Key - default/credential"

# Vault configuration
VAULT_PATH="/Users/alpha/ashco-vault"
VAULT_DEVICE_ID="mini"
